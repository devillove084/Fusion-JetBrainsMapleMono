name: Local Build Only

on:
  workflow_dispatch:

jobs:
  info:
    name: Build instructions
    runs-on: ubuntu-latest
    steps:
      - name: Explain local build policy
        run: |
          echo "This fork builds Lilex Maple Mono locally with build_lilex_maple.sh."
          echo "The original JetBrains Maple Mono scheduled/push workflow is disabled to avoid publishing upstream-style artifacts from this fork."
